/**
 * Single import point for merchant configuration.
 *
 * Swap the imported files to ship a different merchant theme/brand.
 */
export { storeConfig } from "./store.config"
export { themeConfig } from "./theme.config"
export type * from "./types"
